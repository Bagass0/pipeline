def test_average():
    # Example list for testing
    values = [1, 2, 3, 4, 5]
    # Calculate the average
    avg = sum(values) / len(values)
    # Assertion
    assert avg == 3.0, "Average calculation is incorrect"

# You can add more test cases as needed
